export const enum enumTransactionType {
  DEPOSIT = "DEPOSIT",
  WITHDRAWAL = "WITHDRAWAL",
}

export const enum enumCSVColumns {
  TIMESTAMP = "timestamp",
  TYPE = "transaction_type",
  TOKEN = "token",
  AMOUNT = "amount",
}
